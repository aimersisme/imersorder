# iMersOrder r27 — PWA Branding Fix

- Manifest icon URLs now include the active uploaded logo/favicon version.
- Browser metadata icon URL is cache-busted.
- Service worker cache version bumped to r27.
- Existing installed PWAs may need uninstall/reinstall once because the OS can retain the old installed icon snapshot.
