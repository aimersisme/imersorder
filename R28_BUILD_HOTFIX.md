# iMersOrder r28 — Build Hotfix

## Fix
- Fixed Next.js `MetadataRoute.Manifest` TypeScript error in `app/manifest.ts`.
- PWA 512x512 icon now uses valid manifest purpose `maskable` instead of invalid `maskable any`.
- Updated manifest theme color to iMersOrder navy `#071A3A`.
- Bumped release from r27 to r28.

No database migration is required for this build hotfix.
